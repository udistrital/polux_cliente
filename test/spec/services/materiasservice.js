'use strict';

describe('Service: materiasService', function () {

  // load the service's module
  beforeEach(module('poluxApp'));

  // instantiate service
  var materiasService;
  beforeEach(inject(function (_materiasService_) {
    materiasService = _materiasService_;
  }));

  /*it('should do something', function () {
    expect(!!materiasService).toBe(true);
  });*/

});
