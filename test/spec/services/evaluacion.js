'use strict';

describe('Service: evaluacion', function () {

  // load the service's module
  beforeEach(module('poluxApp'));

  // instantiate service
  var evaluacion;
  beforeEach(inject(function (_evaluacion_) {
    evaluacion = _evaluacion_;
  }));

  /*it('should do something', function () {
    expect(!!evaluacion).toBe(true);
  });*/

});
