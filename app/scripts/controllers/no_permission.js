'use strict';

/**
 * @ngdoc controller
 * @name poluxClienteApp.controller:NoPermissionCtrl
 * @description
 * # NoPermissionCtrl
 * Controller of the poluxClienteApp
 * Controlador de la página de permisos insuficientes a la cual es redirigido el usuario cuando intenta acceder a una URL a la cual no tiene permisos
 */
angular.module('poluxClienteApp')
  .controller('NoPermissionCtrl', function () {
  });
